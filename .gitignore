# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# Codex local state
.codex
.codex/

# Cleanup audit artifact (uncommitted scratch)
CLEANUP.md

# Brainstorm/plan scratch (per CLAUDE.md, never committed)
doc/specs/
doc/plans/

# Project-specific
others/
meeting-log.md
configs/models/
presentation/

# CUDA / profiling artifacts
*.ncu-rep
*.nsys-rep
*.ptx
*.cubin
*.fatbin

# Compile/profile caches (runtime artifacts)
.acts_cache/

# Local LLM trace JSONL files (per-run diagnostic capture)
traces/

# Per-invocation run directories (run.log + events.jsonl + traces/)
runs/

# Optimization-memory shared store (JSONL, see doc/memory.md). Accumulates
# distilled lessons across runs; .gitignored so developer-local stores
# don't conflict and write-enabled ablation runs don't dirty the index.
opt_mem/
